<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>

  
       
        <% if @current_user == nil %>
          <li>
          <a href="/">Home</a>
          </li>
          <li>
          <a href="/user_sign_in">Sign in</a>
          </li>
          <li>
          <a href="/user_sign_up">Sign up</a>
          </li>

        <% else %>


          <p style= "text-align:right;"> Welcome, <%= @current_user.username %> </p>
          <li>
          <a href="/">Home</a>
          </li>
        
        <li>
          
          <a href="/user_sign_out">Sign out</a>
        </li>

        <li>
          
          <a href="/edit_user_profile">Your Profile</a>
        </li>

        <li>
          
          <a href="/answers">View Today's Question</a>
        </li>

      

        <% end %>
      </ul>
    <p style="color: red"><%= alert%></p>
    <p style="color: green"><%= notice%></p>

    <%= yield %>
  </body>
</html>
